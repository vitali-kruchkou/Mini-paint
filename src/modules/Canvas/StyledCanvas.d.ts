import styled from 'styled-components'

const Styled = {
    Container: styled.div`
    max-width: 750px;
    max-height: 500px;
    margin:0 auto;
    border:1px solid green;
    `,
}

export default Styled;